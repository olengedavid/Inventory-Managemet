# Script for populating the database. You can run it as:
#
#     mix run priv/repo/seeds.exs
#
# Inside the script, you can read and write to any of your
# repositories directly:
#
#     InventoryManagement.Repo.insert!(%InventoryManagement.SomeSchema{})
#
# We recommend using the bang functions (`insert!`, `update!`
# and so on) as they will fail if something goes wrong.

# alias InventoryManagement.Accounts.Role
# alias InventoryManagement.Accounts.User
# alias InventoryManagement.Accounts

# role = Accounts.create_role(%{name: "retailer"})

# Accounts.create_user(%{
#   role_id: role.id,
#   name: "Limon",
#   email: "limon@example.com",
#   password: "123456"
# })

# role = Account.create_role(%{name: "wholesaler"})

# Accounts.create_user(%{
#   role_id: role.id,
#   name: "David",
#   email: "david@example.com",
#   password: "123456"
# })
